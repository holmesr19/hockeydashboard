import { DisplayName } from 'src/app/interfaces/DisplayName';
import { StatsObj } from 'src/app/interfaces/StatsObj';

export interface TypeSplits {
    'type': DisplayName;
    'splits': StatsObj[];
}
